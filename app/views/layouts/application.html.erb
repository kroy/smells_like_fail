<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= include_gon %>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <!-- Figure out why this is working but just "application" is not -->
    <%= javascript_include_tag "jquery-1.9.1", "jquery_ujs", "highcharts", "bootstrap", "users" %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>

    <!--Load the AJAX API-->

  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>