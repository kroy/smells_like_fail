<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "smells like fail", '#', id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "Help", help_path %></li>
          <li><%= link_to "Recruitment", recruitment_path %></li>
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" id="nubs">
              Nubs <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "All Nubs", users_path %></li>
              <li class="divider"></li>
              <% User.order("nickname ASC").all.each do |user| %>
                <li><%= link_to user.nickname, user_path(user.id) %></li>
              <%end%>
              <li class="divider"></li>
              <li><%= link_to "Don't see your nickname?", '/signup'%></li>
            </ul>
          </li>
        </ul>
      </nav>
    </div>
  </div>
</header>